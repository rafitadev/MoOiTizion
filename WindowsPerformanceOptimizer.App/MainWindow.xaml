<Window x:Class="WindowsPerformanceOptimizer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Windows Performance Optimizer" Height="760" Width="1260" Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="{StaticResource SidebarBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="Windows Performance Optimizer" Foreground="{StaticResource TextBrush}" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,20"/>
                <ToggleButton Content="Dark Theme" IsChecked="{Binding IsDarkTheme}" Margin="0,0,0,18" Height="36"/>
                <Button Content="Overview" Command="{Binding NavigateCommand}" CommandParameter="Overview" Margin="0,0,0,10" Height="38"/>
                <Button Content="Monitoring" Command="{Binding NavigateCommand}" CommandParameter="Monitoring" Margin="0,0,0,10" Height="38"/>
                <Button Content="Profiles" Command="{Binding NavigateCommand}" CommandParameter="Profiles" Margin="0,0,0,10" Height="38"/>
                <Button Content="Advanced" Command="{Binding NavigateCommand}" CommandParameter="Advanced" Margin="0,0,0,10" Height="38"/>
                <Button Content="Logs" Command="{Binding NavigateCommand}" CommandParameter="Logs" Margin="0,0,0,10" Height="38"/>
                <Button Content="Help" Command="{Binding NavigateCommand}" CommandParameter="Help" Height="38"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" Margin="16" CornerRadius="12">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</Window>
