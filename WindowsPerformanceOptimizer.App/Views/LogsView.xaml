<UserControl x:Class="WindowsPerformanceOptimizer.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Action Log" FontSize="28" FontWeight="Bold"/>
        <ListBox Grid.Row="1" Margin="0,18,0,0" ItemsSource="{Binding Entries}"/>
    </Grid>
</UserControl>
