<UserControl x:Class="WindowsPerformanceOptimizer.App.Views.MonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Real-time Monitoring" FontSize="28" FontWeight="Bold"/>
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <GroupBox Header="CPU Series (last 60s)" Margin="6"><ItemsControl ItemsSource="{Binding CpuSeries}"/></GroupBox>
            <GroupBox Header="RAM Series (last 60s)" Grid.Column="1" Margin="6"><ItemsControl ItemsSource="{Binding RamSeries}"/></GroupBox>
        </Grid>
    </Grid>
</UserControl>
