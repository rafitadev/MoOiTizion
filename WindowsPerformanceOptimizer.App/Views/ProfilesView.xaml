<UserControl x:Class="WindowsPerformanceOptimizer.App.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Performance Profiles" FontSize="28" FontWeight="Bold"/>
        <ListBox Grid.Row="1" Margin="0,18,0,0" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}">
            <ListBox.ItemTemplate><DataTemplate><StackPanel Margin="8"><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Text="{Binding Description}" Opacity="0.7"/></StackPanel></DataTemplate></ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Apply" Command="{Binding ApplySelectedCommand}" Width="120" Height="38" Margin="0,0,12,0"/>
            <Button Content="Restore Defaults" Command="{Binding RestoreDefaultsCommand}" Width="160" Height="38"/>
            <TextBlock Text="{Binding Status}" Margin="20,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
